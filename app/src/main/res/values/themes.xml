<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.AiCalCount" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash screen: shown at launch, then switches to Theme.AiCalCount -->
    <style name="Theme.AiCalCount.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/nomnom_amber</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.AiCalCount</item>
    </style>
</resources>